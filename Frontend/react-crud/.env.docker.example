# Use this if you're running the backend via `docker-compose up` instead of `dotnet run`.
# docker-compose.yml maps the backend container to host port 5001 (not 5125).
# Copy this file to .env.local (create-react-app auto-loads .env.local) when using Docker.
REACT_APP_BACKEND_ORIGIN=http://localhost:5001
